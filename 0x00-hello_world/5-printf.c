#include <studio.h>

/**
 * main - prints the phrase with proper grammer,
 * but the outcome is a piece of art,
 * Return: 0 if exited properly, non -zero otherwise
 * */
int main(void)
{
	prinf("with proper grammar, but the outcome is a pieceof art, \n");
	return (0);
}
